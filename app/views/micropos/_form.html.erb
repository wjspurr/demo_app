<%= form_for(@micropo) do |f| %>
  <% if @micropo.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@micropo.errors.count, "error") %> prohibited this micropo from being saved:</h2>

      <ul>
      <% @micropo.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
